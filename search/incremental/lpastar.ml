(** Lifelong Planning A*, implemented from the psuedo code provided in
    the paper on D* lite *)

let search () =
  let calculate_key s = failwith "Stub" in
  let initialize () = failwith "Stub" in
  let updateVertex u = failwith "Stub" in
  let computeShortestPath () = failwith "Stub" in
  let do_search () = failwith "Stub" in
    initialize();
    do_search()

(* EOF *)
